import Home from '../components/Home';
import { Button } from 'evergreen-ui';

const HomePage = props => (
  <div>
    <Home />
    <Button>I am using 🌲!</Button>
  </div>
);

export default HomePage;
